# 专辑详情信息区域重设计方案

## 当前问题
- 大量胶囊样式（pill/badge）导致视觉密集
- 颜色块过多，信息层次不清晰
- 整体显得拥挤和混乱

## 设计目标
- 更轻量、更透气的视觉效果
- 清晰的信息层次
- 保持所有交互功能（点击复制、长按操作）

## 改进方案

### 方案一：精简胶囊 + 文本分组（推荐）
```
┌─────────────────────────────────┐
│ RJ123456  ○ 社团名称            │  ← 精简的胶囊，只保留必要的
├─────────────────────────────────┤
│ 声优                             │
│ 声优A · 声优B · 声优C            │  ← 纯文本 + 分隔符
├─────────────────────────────────┤
│ 标签                             │
│ #标签1  #标签2  #标签3  #标签4   │  ← 纯文本井号标签
└─────────────────────────────────┘
```

**特点：**
- RJ号保留轻量级胶囊（识别度高）
- 社团名称前加图标，无背景
- 声优和标签改为纯文本列表
- 使用分隔线和分组标题来组织信息

### 方案二：列表式布局
```
┌─────────────────────────────────┐
│ RJ       RJ123456               │
│ 社团     社团名称                │
│ 声优     声优A, 声优B, 声优C    │
│ 标签     标签1 · 标签2 · 标签3  │
└─────────────────────────────────┘
```

**特点：**
- 表单式布局，左侧标签，右侧内容
- 完全去除胶囊样式
- 最轻量，但可能稍显单调

### 方案三：混合式（我的推荐）
```
┌─────────────────────────────────┐
│ [RJ123456]  社团名称             │  ← RJ保留方框样式
│                                  │
│ 👤 声优A · 声优B · 声优C         │  ← 图标 + 文本
│                                  │
│ 🏷 标签1 · 标签2 · 标签3 · 标签4 │  ← 图标 + 文本
└─────────────────────────────────┘
```

**特点：**
- RJ号保留边框样式（轻量级）
- 社团名直接显示，无装饰
- 声优和标签使用小图标 + 纯文本
- 用中点分隔，视觉更清爽
- 保持紧凑但不拥挤

## 实施细节

### 颜色方案
- 移除大部分背景色
- 使用 textPrimary/textSecondary 层次
- 保留主题色用于可点击提示（涟漪效果）

### 交互保持
- 点击任意项复制内容
- 长按弹出操作菜单（搜索、创建分组等）
- 使用 `combinedClickable` 实现

### 动画
- 保持现有的淡入/展开动画
- 移除不必要的 animateContentSize

### 响应式
- 内容过长时可横向滚动
- 或使用展开/收起功能

## 推荐实施：方案三（混合式）

优点：
1. 视觉最轻量但不失设计感
2. 信息层次清晰
3. 易于扫视和查找
4. 保持所有功能
5. 实施成本适中
